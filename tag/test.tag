      <wrapper>
        <div></div>

        <style>
          div {
            width: 100%;
            height: 20px;
            background: rgba(200,200,200,0.5);
          }
        </style>
      </wrapper>

      <item>
        <h1>{title}</h1>
        <p>{caption}</p>

        this.title   = opts.title;
        this.caption = opts.caption;
      </item>

      <item2>
        <h2>{ }</h2>
      </item2>